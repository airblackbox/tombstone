# Tombstone runtime data (NEVER commit, may contain keys/ciphertext)
tombstone_data/
*.key
*.blob

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
build/
dist/

# Ledger integrity secrets (NEVER commit the head-signing key)
*.headkey
*.head

# pytest
.pytest_cache/
